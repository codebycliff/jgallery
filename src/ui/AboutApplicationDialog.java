// AboutApplicationDialog.java
package ui;

import javax.swing.JDialog;

/**
 * Class that provides a dialog containing the application details and 
 * specifics.
 */
public class AboutApplicationDialog extends JDialog {
    
    /**
     * Default constructor that creates the about application dialog and
     * and initializes it's components.
     */
    public AboutApplicationDialog() {
        super();
        setTitle("About this Application");
        initComponents();
    }
    
    /**
     * Private helper method that initializes and lays out the dialogs 
     * components.
     */
    private void initComponents() {
        
    }

}
